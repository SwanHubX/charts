apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "swanlab.fullname" . }}-test-connection"
  labels:
    {{- include "swanlab.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "swanlab.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
