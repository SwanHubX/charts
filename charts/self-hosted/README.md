# Self-Hosted

A Helm chart for deploying self-hosted swanlab.

You can install the chart with the command:

```bash
helm repo add swanlab https://helm.swanlab.cn
helm install self-hosted swanlab/self-hosted
```
